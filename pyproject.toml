[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gt-vs-gnn"
version = "0.1.0"
description = "GCN, GAT, and GPS comparison on ogbn-arxiv."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
